rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Lưu ý: Đây là rule demo, không phù hợp cho sản xuất
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
